@page blog.post
@route "/blog/{slug}"
@layout root, blog
@render spa

paths {
  => { slug: "hello-gowdk" }
  => { slug: "compile-first" }
}

view {
  <main>
    <h1>{slug}</h1>
    <p data-slug="{slug}">Dynamic spa route example.</p>
  </main>
}
