package grammar

@page grammar.demo
@route "/demo/{slug}"
@layout root, demo
@render action
@css default page forms

paths {
  => { slug: "starter" }
}

act Submit POST "/demo/{slug}"

view {
  <main>
    <h1 .title>{slug}</h1>
    <form g:post={Submit}>
      <button>Save</button>
    </form>
  </main>
}
